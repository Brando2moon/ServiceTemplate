Manage())return;const {data,error}=await client.from('svc_projects').select('*').order('sort_order').order('created_at',{ascending:false});if(error)return;state.projects=data||[];$('[data-project-list]').innerHTML=state.projects.map(item=>`<article class="content-item"><div><strong>${esc(item.title)}</strong><p>${esc(item.service_category||'Project')} · ${item.published?'Published':'Draft'}</p></div><div class="content-item-actions"><button data-edit-project="${item.id}">Edit</button>${isOwner()?`<button data-delete-project="${item.id}">Delete</button>`:''}</div></article>`).join('')||'<div class="empty-admin">No projects.</div>'}
  function editProject(id){const item=state.projects.find(row=>row.id===id),form=$('[data-project-form]');if(!item)return;for(const key of ['id','title','service_category','location_label','description','before_image_url','after_image_url','completed_on','sort_order'])form.elements[key].value=item[key]??'';form.elements.featured.checked=item.featured;form.elements.published.checked=item.published;form.scrollIntoView({behavior:'smooth'})}
  async function saveProject(event){event.preventDefault();const form=event.currentTarget,status=$('[data-project-status]'),row={title:form.elements.title.value.trim(),service_category:form.elements.service_category.value.trim()||null,location_label:form.elements.location_label.value.trim()||null,description:form.elements.description.value.trim()||null,before_image_url:form.elements.before_image_url.value.trim(),after_image_url:form.elements.after_image_url.value.trim(),completed_on:form.elements.completed_on.value||null,sort_order:Number(form.elements.sort_order.value||0),featured:form.elements.featured.checked,published:form.elements.published.checked};const result=form.elements.id.value?await client.from('svc_projects').update(row).eq('id',form.elements.id.value):await client.from('svc_projects').insert(row);status.textContent=result.error?result.error.message:'Project saved.';if(!result.error){form.re